# =============================================================
# Voice Assistant — Direct dependencies (Python 3.13, CPU-only)
# Install with:  pip install -r requirements.txt
# (PyTorch is installed separately by setup.ps1 from the CPU index)
# =============================================================

# --- App stack ---
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart
python-dotenv
google-genai>=2.0
piper-tts>=1.4
streamlit>=1.33
sounddevice

# --- NeMo core deps ---
omegaconf>=2.3
hydra-core>=1.3
lhotse>=1.32.2
transformers>=4.30
huggingface_hub>=0.24
soundfile
librosa>=0.10.1
scipy
numpy
packaging
lightning>=2.2.1
pytorch-lightning>=2.2.1
text-unidecode
inflect
numexpr<2.14.0
ruamel.yaml
wget
wrapt
python-dateutil
onnx>=1.7.0
tensorboard
einops
sentencepiece
kaldialign
pyannote.core
pyannote.metrics
braceexpand
webdataset

# --- NeMo optional loggers (referenced in code but optional) ---
neptune
mlflow
wandb

# --- Data tooling pinned for compatibility ---
pyarrow>=15.0,<19.0
datasets<3.0
